- name: Remove bootstrap token secret
  ansible.builtin.command:
    cmd: "kubectl -n kube-system delete secret bootstrap-token-{{ bootstrap_token_id }}"
  changed_when: true
  when: inventory_hostname == 'master1'
